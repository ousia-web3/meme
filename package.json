// package.json 일부 예시
{
  "name": "ousia-landing",
  "version": "1.0.0",
  "homepage": "https://ousia-web3.github.io/meme", // ✅ 추가

  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",

    "predeploy": "npm run build",        // ✅ 빌드 먼저
    "deploy": "gh-pages -d dist"         // ✅ dist 폴더를 gh-pages 브랜치로 배포
  },

  "devDependencies": {
    "gh-pages": "^5.0.0",               // ✅ 포함되어야 함
    "vite": "^5.0.0",
    ...
  }
}
